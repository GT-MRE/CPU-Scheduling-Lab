# CPU Scheduling Lab

Streamlit app for simulating classic CPU scheduling algorithms and visualizing the resulting Gantt chart.

## Run locally

```bash
pip install -r requirements.txt
streamlit run app.py
```

